doctype html
html(lang="ja")
  head
    meta(charset="utf-8")
  body
    h1 新規登録
    form(action="/signin", method="POST")
      input(type="text", name="name", palaceholder="名前")
      input(type="password", name="password", palaceholder="パスワード")
      button(type="submit") 送信